# Ruby1R1ng.github.io

This repository now uses [Hugo](https://gohugo.io/) with the Blowfish theme.

## Local preview

1. Install Hugo Extended.
2. Run:

```bash
hugo server -D
```

## Deploy

Push to `main`, then GitHub Actions workflow `.github/workflows/hugo.yml` will build and deploy to GitHub Pages.
